# Lock file generado con `pip freeze` desde el entorno de desarrollo (Ruben, ago-2026).
# Uso: pip install -r requirements-lock.txt
# Reproduce EXACTAMENTE el entorno donde se probó el pipeline completo
# (migración SQL, entrenamiento, inferencia con logging).
# Para instalación mínima sin herramientas de editor/Jupyter, usar requirements.txt.

# --- Dependencias directas del proyecto ---
pandas==3.0.5
SQLAlchemy==2.0.51
pyodbc==5.3.0
scikit-learn==1.9.0
imbalanced-learn==0.14.2
xgboost==3.3.0
joblib==1.5.3
matplotlib==3.11.1
seaborn==0.13.2

# --- Dependencias transitivas relevantes (fijadas para reproducibilidad exacta) ---
numpy==2.5.1
scipy==1.18.0
threadpoolctl==3.6.0
sklearn-compat==0.1.6
fonttools==4.63.0
kiwisolver==1.5.0
cycler==0.12.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pillow==12.3.0
packaging==26.2
six==1.17.0
tzdata==2026.3
greenlet==3.5.4
typing_extensions==4.16.0

# --- NO incluidas a propósito ---
# Los siguientes paquetes presentes en el pip freeze original del entorno de
# desarrollo corresponden a herramientas del editor (Jupyter/VS Code) y NO son
# necesarios para ejecutar ningun script de este repositorio:
# jupyterlab, notebook, ipykernel, jupytext, nbdime, jupyterlab_git,
# nbconvert, nbformat, nbclient, jupyter_server*, jupyter-lsp, jupyter_client,
# jupyter_core, jupyter-events, GitPython, gitdb, y sus dependencias asociadas
# (tornado, pyzmq, terminado, prometheus_client, websocket-client, etc.)
